<UserControl x:Class="D2G.Iris.ML.ConfigUI.WPF.Views.PairPlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="http://schemas.abtsoftware.co.uk/scichart"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Pair Plot Analysis" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <!-- Column Selection -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Select Column:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding NumericColumns}"
                              SelectedItem="{Binding SelectedColumnToAdd}"
                              DisplayMemberPath="."
                              Height="30"/>

                    <Button Grid.Column="3"
                            Content="Add Column"
                            Command="{Binding AddColumnCommand}"
                            Height="30"
                            Padding="15,0"
                            Background="#FF4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF66BB6A"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#FFCCCCCC"/>
                                        <Setter Property="Foreground" Value="#FF888888"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>

                <!-- Selected Columns Display -->
                <StackPanel Grid.Row="2">
                    <TextBlock Text="Selected Columns (2-5):" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ItemsControl ItemsSource="{Binding SelectedColumns}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#E3F2FD" BorderBrush="#2196F3" BorderThickness="1"
                                        Padding="8,4" Margin="0,0,5,5" CornerRadius="3">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding}" FontSize="12" Foreground="#1976D2" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <Button Content="×"
                                                Command="{Binding DataContext.RemoveColumnCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Foreground="#F44336"
                                                FontSize="16"
                                                FontWeight="Bold"
                                                Padding="4,0"
                                                Cursor="Hand"
                                                VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button Content="Generate Pair Plot"
                            Command="{Binding GeneratePlotCommand}"
                            Height="35"
                            Padding="20,0"
                            Margin="0,10,0,0"
                            Background="#FF4682B4"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="Bold"
                            FontSize="13"
                            HorizontalAlignment="Left"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF5A9BD5"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#FFCCCCCC"/>
                                        <Setter Property="Foreground" Value="#FF888888"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Pair Plot Grid -->
        <Grid Grid.Row="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding PairPlotCharts}" Margin="15" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding SelectedColumns.Count}"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDDDDD" BorderThickness="1" Margin="5" Background="White">
                                <Grid Width="350" Height="350">
                                    <!-- Scatter Plot -->
                                    <s:SciChartSurface>
                                        <s:SciChartSurface.Style>
                                            <Style TargetType="s:SciChartSurface">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHistogram}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </s:SciChartSurface.Style>
                                        <s:SciChartSurface.XAxis>
                                            <s:NumericAxis DrawMajorBands="True"
                                                           GrowBy="0.05, 0.05"
                                                           AutoRange="Always"
                                                           DrawMinorTicks="True"
                                                           DrawMajorGridLines="True"
                                                           DrawMinorGridLines="False"
                                                           FontSize="11"
                                                           AxisTitle="{Binding XColumn}"/>
                                        </s:SciChartSurface.XAxis>
                                        <s:SciChartSurface.YAxis>
                                            <s:NumericAxis DrawMajorBands="True"
                                                           GrowBy="0.05, 0.05"
                                                           AutoRange="Always"
                                                           DrawMinorTicks="True"
                                                           DrawMajorGridLines="True"
                                                           DrawMinorGridLines="False"
                                                           FontSize="11"
                                                           AxisTitle="{Binding YColumn}"/>
                                        </s:SciChartSurface.YAxis>
                                        <s:SciChartSurface.RenderableSeries>
                                            <s:XyScatterRenderableSeries DataSeries="{Binding ScatterPlotData}"
                                                                         Stroke="#FF4682B4"
                                                                         StrokeThickness="1">
                                                <s:XyScatterRenderableSeries.PointMarker>
                                                    <s:EllipsePointMarker Width="5" Height="5" Fill="#FF4682B4" Stroke="#FF4682B4" StrokeThickness="1"/>
                                                </s:XyScatterRenderableSeries.PointMarker>
                                            </s:XyScatterRenderableSeries>
                                        </s:SciChartSurface.RenderableSeries>
                                        <s:SciChartSurface.ChartModifier>
                                            <s:ModifierGroup>
                                                <s:ZoomPanModifier/>
                                                <s:MouseWheelZoomModifier/>
                                                <s:ZoomExtentsModifier/>
                                            </s:ModifierGroup>
                                        </s:SciChartSurface.ChartModifier>
                                    </s:SciChartSurface>

                                    <!-- Histogram (for diagonal) -->
                                    <s:SciChartSurface>
                                        <s:SciChartSurface.Style>
                                            <Style TargetType="s:SciChartSurface">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsHistogram}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </s:SciChartSurface.Style>
                                        <s:SciChartSurface.XAxis>
                                            <s:NumericAxis DrawMajorBands="True"
                                                           GrowBy="0.05, 0.05"
                                                           AutoRange="Always"
                                                           DrawMinorTicks="True"
                                                           DrawMajorGridLines="True"
                                                           DrawMinorGridLines="False"
                                                           FontSize="11"
                                                           AxisTitle="{Binding XColumn}"/>
                                        </s:SciChartSurface.XAxis>
                                        <s:SciChartSurface.YAxis>
                                            <s:NumericAxis DrawMajorBands="True"
                                                           GrowBy="0.05, 0.05"
                                                           AutoRange="Always"
                                                           DrawMinorTicks="True"
                                                           DrawMajorGridLines="True"
                                                           DrawMinorGridLines="False"
                                                           FontSize="11"
                                                           AxisTitle="Frequency"/>
                                        </s:SciChartSurface.YAxis>
                                        <s:SciChartSurface.RenderableSeries>
                                            <s:FastColumnRenderableSeries DataSeries="{Binding HistogramData}"
                                                                           Fill="#FF66BB6A"
                                                                           Stroke="#FF4CAF50"
                                                                           StrokeThickness="1"
                                                                           DataPointWidth="1.0"/>
                                        </s:SciChartSurface.RenderableSeries>
                                        <s:SciChartSurface.ChartModifier>
                                            <s:ModifierGroup>
                                                <s:ZoomPanModifier/>
                                                <s:MouseWheelZoomModifier/>
                                                <s:ZoomExtentsModifier/>
                                            </s:ModifierGroup>
                                        </s:SciChartSurface.ChartModifier>
                                    </s:SciChartSurface>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Loading Overlay -->
            <Border Background="#80FFFFFF"
                    Visibility="{Binding IsGeneratingPlot, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="6" Margin="0,0,0,15"/>
                    <TextBlock Text="{Binding ProgressMessage}" FontSize="14" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Empty State -->
            <Border Background="#FAFAFA">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding PairPlotCharts.Count}" Value="0">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="Select 2-5 numeric columns and click Generate Pair Plot"
                               FontSize="14"
                               Foreground="#888888"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Pair plots show relationships between all pairs of selected variables"
                               FontSize="12"
                               Foreground="#AAAAAA"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
