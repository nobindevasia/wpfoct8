<UserControl x:Class="D2G.Iris.ML.ConfigUI.WPF.Views.ClusterVisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="http://schemas.abtsoftware.co.uk/scichart"
             xmlns:converters="clr-namespace:D2G.Iris.ML.ConfigUI.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>

        <!-- Point Marker Template for Scatter Plot -->
        <ControlTemplate x:Key="PointMarkerTemplate" TargetType="s:BasePointMarker">
            <Ellipse Width="8" Height="8"
                     Fill="{Binding Stroke, RelativeSource={RelativeSource AncestorType=s:BaseRenderableSeries}}"
                     Stroke="White" StrokeThickness="1"/>
        </ControlTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Cluster Visualization"
                   FontSize="18" FontWeight="Bold"
                   Margin="10"/>

        <!-- Chart Area -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" Margin="10">
            <Grid>
                <!-- SciChart -->
                <s:SciChartSurface x:Name="ClusterChart" Visibility="{Binding HasData, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <s:SciChartSurface.XAxis>
                        <s:NumericAxis AxisTitle="Feature 1" />
                    </s:SciChartSurface.XAxis>
                    <s:SciChartSurface.YAxis>
                        <s:NumericAxis AxisTitle="Feature 2" />
                    </s:SciChartSurface.YAxis>
                    <s:SciChartSurface.ChartModifier>
                        <s:ModifierGroup>
                            <s:ZoomPanModifier />
                            <s:MouseWheelZoomModifier />
                            <s:ZoomExtentsModifier />
                            <s:LegendModifier ShowLegend="True" />
                        </s:ModifierGroup>
                    </s:SciChartSurface.ChartModifier>
                </s:SciChartSurface>

                <!-- No Data Message -->
                <TextBlock Text="Run clustering to see visualization here."
                           FontSize="16"
                           Foreground="Gray"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="{Binding HasData, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <!-- Summary Panel -->
        <GroupBox Grid.Row="2" Header="Cluster Summary" Margin="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="200">
                <TextBlock Text="{Binding ClusterSummary}"
                           FontFamily="Consolas"
                           Margin="10"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
